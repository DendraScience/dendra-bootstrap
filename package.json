{
  "name": "dendra-bootstrap",
  "version": "0.0.3",
  "description": "Custom Bootstrap 4 variables.",
  "license": "BSD-2-Clause-FreeBSD",
  "author": "J. Scott Smith <scott@newleafsolutionsinc.com>",
  "scripts": {
    "build": "test \"$BOOTSTRAP\" && cwd=$(pwd) && cp -f ./scss/*.scss \"$BOOTSTRAP/scss\" && ( cd \"$BOOTSTRAP\"; grunt; cp -R ./dist \"$cwd\"; exit 0; ) && cd \"$cwd\"",
    "clean": "rm -r dist/*"
  }
}
